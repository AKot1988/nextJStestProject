// `app/dashboard/page.tsx` is the UI for the `/dashboard` URL
import classes from './page.module.scss';

export default function Page() {
  return <h1 className={classes.text}>Hello, Dashboard Page!</h1>;
}
